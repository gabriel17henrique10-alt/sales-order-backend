{
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^/sales-order/(.*)$",
            "destination": "srv-api",
            "authenticationType": "xsuaa",
            "csrfProtection": true
        }
    ]
}